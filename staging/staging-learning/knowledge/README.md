# Knowledge Buckets

Separate knowledge by trust level:

- `canonical/` — reviewed, trusted, durable
- `staged/` — useful but not fully verified
- `disputed/` — conflicting or unresolved
- `rejected/` — disproven or obsolete
- `speculative/` — theories, possibilities, future branches

Use these buckets to stop raw learning material from turning into accidental truth.
