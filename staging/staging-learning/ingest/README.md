# Ingest

Machine-facing derived artifacts live here.

Subfolders:
- `manifests/` — provenance and bundle manifests
- `jsonl/` — machine-ingest records
- `queues/` — pending ingest work
- `rules/` — ingest-side policy fragments

Rule: markdown truth first, ingest second.
